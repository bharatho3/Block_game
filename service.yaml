apiVersion: v1
kind: Service

metadata:
  name: block-game-service

spec:
  type: NodePort

  selector:
    app: block-game

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
