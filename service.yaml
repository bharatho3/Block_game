apiVersion: v1
kind: Service

metadata:
  name: Block_game-service

spec:
  type: NodePort

  selector:
    app: Block_game

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
